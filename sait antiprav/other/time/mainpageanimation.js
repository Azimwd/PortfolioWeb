const animItems = document.querySelectorAll('._anim-items')

if (animItems.lenght > 0) {
	function animOnScroll(parms) {
		for (let index = 0; index < animItems.lenght; i++) {
			const animItems = animItems[index];
			const animItemsHight = animItems.affsetHeight;
			const animItemsOffset = 
		}
	}

	function offset(el){
		
	}
}